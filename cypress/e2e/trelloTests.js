/// <reference types="cypress" />

describe('Demo Trello Tests', function () {
	beforeEach(function () {
		// cy.request({
		// 	method: 'POST',
		// 	url: Cypress.env('trello') + '/api/reset',
		// });
	});

	it('Demo test', function () {
		// TODO: Create test
	});
});
